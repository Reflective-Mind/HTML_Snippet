{
  "name": "html-viewer-playground",
  "version": "1.0.0",
  "description": "A simple HTML viewer and playground with no login requirements",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install",
    "deploy:render": "git push render main",
    "deploy:vercel": "vercel --prod"
  },
  "dependencies": {
    "compression": "^1.8.0",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 